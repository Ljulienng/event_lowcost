  <nav class="navbar navbar-expand-md navbar-dark bg-dark">
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav mr-auto" style="font-size: 1.5em">
            <li class="nav-item active">
              <a class="nav-link" href="<%= events_path%>" style = "color: orange">EventLite<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
              <%= link_to "Create event", new_event_path, :class => "nav-link"%>
            </li>
          </ul>
        </div>
        <div class="navbar-nav flex-row ml-md-auto d-none d-md-flex">
            <div class = "nav-item dropdown">
              <a class="nav-link dropdown-toggle pt-3" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><i class="fas fa-align-justify fa-2x"></i></a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                  <% if user_signed_in? %>
                    <%= link_to "Sign out", destroy_user_session_path, method: :delete, :class => "dropdown-item"  %>
                    <%= link_to "Secret page", home_secret_path, :class => "dropdown-item"  %>
                    <%= link_to "Edit email / password", edit_user_registration_path, :class => "dropdown-item"%>
                    <%= link_to "Profile", user_path(current_user.id), :class => "dropdown-item"%>
                      <% if current_user.try(:admin?) %>
                        <%= link_to "Admin dashboard", admins_root_path, class: "dropdown-item" %>
                      <% end %>
                  <% else %>
                      <%= link_to "Sign in", new_user_session_path, :class => "dropdown-item" %>
                      <%= link_to "Sign up", new_user_registration_path , :class => "dropdown-item" %>
                  <% end %>
                </div>
            </div>
        </div>
    </nav>

    <script>
      $(function () {
        $(".dropdown-toggle").dropdown();
      })
    </script>
